pre-commit:
  parallel: true
  commands:
    security-misc:
      run: gitleaks protect --verbose --staged
    osv-scanner:
      tags: security
      run: osv-scanner scan --recursive {staged_files}
      glob:
        - "uv.lock"
        - "pnpm-lock.yaml"
